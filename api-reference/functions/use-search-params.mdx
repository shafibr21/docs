---
title: useSearchParams
---

# useSearchParams

Hook that returns the current URLSearchParams in client components.

```js
import { useSearchParams } from "next/navigation";

const sp = useSearchParams();
const q = sp.get("q");
```
