---
title: NextResponse
---

# NextResponse

`NextResponse` is a helper to create responses in middleware and server functions, with helpers for redirects, rewrites and setting cookies.

```js
import { NextResponse } from "next/server";

return NextResponse.redirect("/login");
```
