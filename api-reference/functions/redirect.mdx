---
title: redirect
---

# redirect

Perform a temporary redirect (302) from server code or middleware. Use `redirect()` with the target path.

```js
import { redirect } from "next/navigation";

return redirect("/login");
```
